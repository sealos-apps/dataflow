import { FindBarProvider, useFindBar } from './FindBar.Provider'
import { FindBarBar } from './FindBar.Bar'

export const FindBar = {
  Provider: FindBarProvider,
  Bar: FindBarBar,
}

export { useFindBar }
