mutation BootstrapSealosSession($input: SealosBootstrapInput!) {
  BootstrapSealosSession(input: $input) {
    sessionToken
    expiresAt
    type
    hostname
    port
    database
    displayName
  }
}
